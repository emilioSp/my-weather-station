/*import type {Meter} from "./types.ts";

export const MeterFactory = (meter: Meter) => {
  if (meter.type === 'indoor') {
    return new OutDoorMeter(meter);
  }
  if (meter.type === 'outdoor') {
    return new IndoorMeter(meter);
  }

  throw new Error(`Unknown meter type ${meter.type}`);
}*/
